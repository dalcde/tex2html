name: separator
<hr />

name: useimager
<div class="imager_output">
{% if obj.renderer.vectorImager.enabled %}
<img src="{{ obj.vectorImage.url }}" alt="{{ obj.source|e }}" />
{% else %}
<img src="{{ obj.image.url }}" alt="{{ obj.source|e }}" />
{% endif %}
</div>

name: fakeqed
<span class="qedhere">&squ;</span><span class="clear"></span>

name: tph
{{ obj.html }}
